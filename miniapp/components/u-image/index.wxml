<view class="image-view {{ radius ? 'radius' : '' }}" style="{{ src == 'null' ? 'background: #f5f5f5' : '' }}">
	<view class="qx-img" wx:if="{{ src == 'null' }}"></view>
	<image wx:else bindtap="preview" class="img" src="{{ src }}" lazy-load="{{ true }}" mode="{{ mode }}" />
</view>